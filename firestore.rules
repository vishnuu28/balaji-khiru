rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny. Open specific collections when your app needs them.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
