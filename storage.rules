rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default deny. Allow paths explicitly when needed.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
